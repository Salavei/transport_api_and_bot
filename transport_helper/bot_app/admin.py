from django.contrib import admin
from bot_app.models import Profile, SelectedStation, SelectedTransport

admin.site.register(Profile)
admin.site.register(SelectedStation)
admin.site.register(SelectedTransport)

